# Docket Desktop

Native invoice and receipt review app built on the `docket-idp` Python library.

From this repository, install the library and the app separately:

```bash
python -m pip install -e . -e apps/desktop
docket-desktop
```

See [the desktop guide](../../docs/DESKTOP.md) for usage and macOS build steps.
